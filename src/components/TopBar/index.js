import React from 'react';
import Typography from "@mui/material/Typography"
import "./index.css"
const TopBar = () => {
  return <div>
      <Typography className="layout" style={{fontSize: "20px", fontWeight: "500"}}>Reporting</Typography>
  </div>;
};

export default TopBar;
